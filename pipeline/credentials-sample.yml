CF_DOMAINS: "*.sys.example.com,*.cfapps.example.com,*.uaa.sys.example.com,*.pks.example.com"
GCP_CREDENTIALS: |
    {
      // insert GCP service account here
    }
GCP_DNS_WAIT: 120
GCP_HTTPS_PROXY: // run `gcloud compute target-https-proxies list` to get this
LE_EMAIL: me@domain.com
OPSMAN_CERT_NAME: Certificate
PCF_OPSMGR: "https://opsman.xxx.yyy"
PCF_PASSWD: password123
PCF_USER: admin

